namespace NovaEngine.Windowing
{
    /// <summary>The states a window can be in.</summary>
    public enum WindowState
    {
        /// <summary>The window is in it's normal state.</summary>
        Normal,

        /// <summary>The window is minimised.</summary>
        Minimised,

        /// <summary>The window is maximised.</summary>
        Maximised
    }
}
